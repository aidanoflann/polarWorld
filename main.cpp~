//g++ *.cpp -o ex -std=c++0x -lSDL2 -lSDL2_gfx

#include "game.h"

double radPerDeg = 0.01745329251;

int main()
{
	//instantiate the game class
	Game game;
	//initialise
	game.init();
	//run (main game loop)
	game.run();
	
	return 0;
}